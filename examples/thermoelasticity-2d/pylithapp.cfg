[pylithapp.metadata]
# This is not a self-contained simulation configuration file. This
# file only specifies the general parameters common to the simulations
# in this directory.
keywords = [example, 2D, thermoelasticity, thermal stress, heat conduction]
features = [
    Triangular cells,
    pylith.meshio.MeshIOPetsc,
    pylith.problems.TimeDependent,
    pylith.materials.Thermoelasticity,
    pylith.materials.IsotropicLinearThermoelasticity,
    pylith.bc.DirichletTimeDependent,
    spatialdata.spatialdb.UniformDB,
    pylith.meshio.DataWriterHDF5
    ]

# ----------------------------------------------------------------------
# journal
# ----------------------------------------------------------------------
[pylithapp.journal.info]
pylithapp = 1
timedependent = 1
solution = 1
meshiopetsc = 1
thermoelasticity = 1
isotropiclinearthermoelasticity = 1
dirichlettimedependent = 1
petscoptions = 1

# ----------------------------------------------------------------------
# mesh_generator
# ----------------------------------------------------------------------
[pylithapp.mesh_generator]
reader = pylith.meshio.MeshIOPetsc
reader.filename = mesh_tri.msh
reader.coordsys.space_dim = 2

# ----------------------------------------------------------------------
# problem
# ----------------------------------------------------------------------
[pylithapp.problem]
# Set defaults for output
defaults.name = thermoelasticity

[pylithapp.problem.defaults]
quadrature_order = 2

# Output the solution over the domain
[pylithapp.problem]
solution_observers = [domain]

# ----------------------------------------------------------------------
# materials
# ----------------------------------------------------------------------
# We use thermoelastic material with uniform properties.
[pylithapp.problem]
materials = [crust]
materials.crust = pylith.materials.Thermoelasticity

[pylithapp.problem.materials.crust]
description = Thermoelastic crust
label_value = 1

# Material properties database
db_auxiliary_field = spatialdata.spatialdb.UniformDB
db_auxiliary_field.description = Thermoelastic material properties
db_auxiliary_field.values = [density, vs, vp, specific_heat, thermal_conductivity, reference_temperature, thermal_expansion_coefficient]
db_auxiliary_field.data = [2700.0*kg/m**3, 3500.0*m/s, 6000.0*m/s, 900.0*J/(kg*K), 3.0*W/(m*K), 300.0*K, 2.4e-5*1/K]

# Auxiliary subfield discretization
auxiliary_subfields.density.basis_order = 0
auxiliary_subfields.specific_heat.basis_order = 0
auxiliary_subfields.thermal_conductivity.basis_order = 0
auxiliary_subfields.reference_temperature.basis_order = 0
auxiliary_subfields.thermal_expansion_coefficient.basis_order = 0

bulk_rheology = pylith.materials.IsotropicLinearThermoelasticity
bulk_rheology.auxiliary_subfields.bulk_modulus.basis_order = 0
bulk_rheology.auxiliary_subfields.shear_modulus.basis_order = 0

# Derived fields for output
derived_subfields.cauchy_strain.basis_order = 0
derived_subfields.cauchy_stress.basis_order = 0

# ----------------------------------------------------------------------
# boundary conditions
# ----------------------------------------------------------------------
# We fix displacement on the boundaries and apply temperature
# boundary conditions that represent a geothermal gradient.
[pylithapp.problem]
bc = [bc_left_disp, bc_right_disp, bc_bottom_disp, bc_bottom_temp, bc_top_temp]
bc.bc_left_disp = pylith.bc.DirichletTimeDependent
bc.bc_right_disp = pylith.bc.DirichletTimeDependent
bc.bc_bottom_disp = pylith.bc.DirichletTimeDependent
bc.bc_bottom_temp = pylith.bc.DirichletTimeDependent
bc.bc_top_temp = pylith.bc.DirichletTimeDependent

# Left boundary: fix horizontal displacement
[pylithapp.problem.bc.bc_left_disp]
label = boundary_left
label_value = 10
constrained_dof = [0]
field = displacement
db_auxiliary_field = pylith.bc.ZeroDB
db_auxiliary_field.description = Dirichlet BC on left boundary

# Right boundary: fix horizontal displacement
[pylithapp.problem.bc.bc_right_disp]
label = boundary_right
label_value = 11
constrained_dof = [0]
field = displacement
db_auxiliary_field = pylith.bc.ZeroDB
db_auxiliary_field.description = Dirichlet BC on right boundary

# Bottom boundary: fix vertical displacement
[pylithapp.problem.bc.bc_bottom_disp]
label = boundary_bottom
label_value = 12
constrained_dof = [1]
field = displacement
db_auxiliary_field = pylith.bc.ZeroDB
db_auxiliary_field.description = Dirichlet BC on bottom boundary

# Bottom boundary: fixed temperature (hot, geothermal gradient)
[pylithapp.problem.bc.bc_bottom_temp]
label = boundary_bottom
label_value = 12
constrained_dof = [0]
field = temperature
db_auxiliary_field = spatialdata.spatialdb.UniformDB
db_auxiliary_field.description = Temperature BC on bottom boundary
db_auxiliary_field.values = [initial_amplitude]
db_auxiliary_field.data = [600.0*K]

# Top boundary: fixed temperature (surface temperature)
[pylithapp.problem.bc.bc_top_temp]
label = boundary_top
label_value = 13
constrained_dof = [0]
field = temperature
db_auxiliary_field = spatialdata.spatialdb.UniformDB
db_auxiliary_field.description = Temperature BC on top boundary (surface)
db_auxiliary_field.values = [initial_amplitude]
db_auxiliary_field.data = [300.0*K]

# ----------------------------------------------------------------------
# initial conditions
# ----------------------------------------------------------------------
# Set the initial temperature field to a linear geothermal gradient.
[pylithapp.problem]
ic = [temperature_ic]
ic.temperature_ic = pylith.problems.InitialConditionDomain

[pylithapp.problem.ic.temperature_ic]
subfields = [temperature]

db = spatialdata.spatialdb.SimpleGridDB
db.description = Initial temperature (geothermal gradient)
db.filename = initial_temperature.spatialdb
db.query_type = linear

# ----------------------------------------------------------------------
# PETSc
# ----------------------------------------------------------------------
[pylithapp.petsc]
ts_type = beuler

# Use fieldsplit preconditioner for the coupled system
pc_type = fieldsplit
pc_fieldsplit_type = additive

fieldsplit_displacement_pc_type = lu
fieldsplit_temperature_pc_type = lu

ksp_rtol = 1.0e-10
ksp_atol = 1.0e-15
ksp_max_it = 200
ksp_gmres_restart = 50
ksp_error_if_not_converged = true

snes_rtol = 1.0e-10
snes_atol = 1.0e-10
snes_error_if_not_converged = true

# End of file
